$(document).ready(function() {
  // escribimos nuestor código aquí

  // Selectores en JQUERY

  // SELECCIONAR UNA ETIQUETA

  $('h1');
  // $('h1').css({color: 'salmon'});

  // SELECCIONAR VARIAS ETIQUETAS

  $('h1, p');
  // $('h1, p').hide();

  // SELECCIONAR POR ID

  $('#segundo');
  // $('#segundo').css({color: 'yellow'});

  // SELECCIONAR POR CLASE

  $('.texto');
  // $('.texto').css({background: 'salmon'});

  $('.contenedor a');
  // $('.contenedor a').hide();

});
